# MCP Connector Reference

`regulatory-legal.json` is the upstream Claude `.mcp.json` archived for reference only.
Codex v1 does not enable these remote connectors automatically. Configure Codex MCP servers separately before relying on any remote system listed here.
Until then, use local files, local exports, or pasted text with the legal skills.
